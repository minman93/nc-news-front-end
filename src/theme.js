import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0F9FB7",
      secondary: "#F8F8F8",
    },
  },
  background: {
    default: "#fff",
  },
});
export default theme;
